# build/run native on your Mac; CI can still use amd64
services:
  gateway-java-spring:       { build: { platforms: [] } }
  register-java-spring:      { build: { platforms: [] } }
  mobile-kotlin-spring:      { build: { platforms: [] } }
  barcodes-go:               { build: { platforms: [] } }
  autocomplete-go:           { build: { platforms: [] } }
  analytics-go:              { build: { platforms: [] } }
  research-go:               { build: { platforms: [] } }
  frontend-react:            { build: { platforms: [] } }